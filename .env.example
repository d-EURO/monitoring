# Database Configuration
DB_HOST=localhost
DB_PORT=5432
DB_NAME=deuro_monitoring
DB_USER=postgres
DB_PASSWORD=your-secure-password
DB_SSL=false

# Or use a connection string instead (recommended for production)
# DATABASE_URL=postgresql://user:password@host:5432/database?sslmode=require

# Blockchain Configuration
RPC_URL=https://your-rpc-provider.com/v2/your-api-key
DEPLOYMENT_BLOCK=22300000
BLOCKCHAIN_ID=1

# API Configuration
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:5173

# Price Cache Configuration (in milliseconds)
PRICE_CACHE_TTL_MS=120000

# Database Pool Configuration
PG_MAX_CLIENTS=10